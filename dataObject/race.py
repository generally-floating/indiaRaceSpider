class Race:
    def __init__(self, raceInfo, winnerContent, record, raceDetails, raceHouse, dividends, racingIncidents):
        self.raceInfo = raceInfo
        self.winnerContent = winnerContent
        self.record = record
        self.raceDetails = raceDetails
        self.raceHouse = raceHouse
        self.dividends = dividends
        self.racingIncidents = racingIncidents